{
  "buildCommand": "echo 'Working directory:' && pwd && echo 'Files in directory:' && ls -la && echo '=== Checking repository structure ===' && if [ -f '../package.json' ]; then echo 'Found package.json in parent directory'; cp ../package.json ./package.json; cp -r ../app ./ 2>/dev/null || echo 'App directory not found in parent'; cp -r ../components ./ 2>/dev/null || echo 'Components directory not found in parent'; fi && echo '=== After setup ===' && ls -la && echo '=== Building Next.js ===' && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  }
}